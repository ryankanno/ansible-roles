python:
  package: python2.7
  version: 2.7.3-5ubuntu4.3
  libraries:
    latest:
      - { package: 'python-dev' }
      - { package: 'python-pip' }
      - { package: 'python-virtualenv' }
    versioned:
      # fill me, seymour

env:
  proxies:
    http_proxy:
    https_proxy:
    ftp_proxy:
