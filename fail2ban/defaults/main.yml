fail2ban:
  package: fail2ban
  version: 0.8.11-1
  config:
    src: 'jail.local.j2'
    ignoreip: 127.0.0.1/8
    bantime: 600
    banaction: iptables-multiport
    backend: auto
    action: '%(action_mwl)s'
    destemail: root@localhost

env:
  http_proxy:
  https_proxy:
  ftp_proxy:
