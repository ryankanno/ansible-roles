redis:
  version: 2:2.4.15-1
  config_dir: /etc/redis
  user: redis
  group: redis
  config:
    port: 6379
    password: 
    pid_file: /var/run/redis.pid
    num_databases: 16
    working_dir: /var/db/redis
